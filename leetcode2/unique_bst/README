Find the number of unique bst for nodes store values 1..n

-- Recursion. 
-- Use each number as root, recursion on left and right to accumulate count. 
-- Only need to recursion on the size. Dont need to care about exact values for each subproblem. 
